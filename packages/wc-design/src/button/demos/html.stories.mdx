import { html } from 'lit';
import { Meta, Story, Canvas, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import '../index.ts';
import '../../space';
import { BasicDemo } from './basic.ts';
import { SuccessDemo, WarningDemo, DangerDemo } from './status.ts';

<Meta title="按钮 Button" component={'wc-button'} />

### 基本用法

按钮分为 主要按钮、次要按钮、常规按钮、虚线按钮和文本按钮五种。

<Canvas>
  <Story name="基本用法">{BasicDemo}</Story>
</Canvas>

### 按钮状态

按钮状态分为 警告，危险，成功 三种，可以与按钮类型同时生效。

<Canvas>
  <Story name="按钮状态">{SuccessDemo}</Story>
  <Story name="sidebar-hidden-button-status-warning">{WarningDemo}</Story>
  <Story name="sidebar-hidden-button-status-danger">{DangerDemo}</Story>
  <Source
    language="css"
    dark
    format={false}
    code={dedent`
     .container {
       display: grid | inline-grid;
     }
  `}
  />
</Canvas>
